# 开发进度面板

## 当前目标

完成一个可演示的端云语音闭环：

`实时麦克风 -> DashScope ASR -> DeepSeek -> DashScope TTS -> 扬声器播放`

## 当前状态

- 项目主线已完全收敛到语音闭环
- 独立项目目录已切换到 `2026AIoT`
- 当前阶段：实时语音闭环稳定化

## 任务看板

| 任务 | 状态 | 说明 |
|---|---|---|
| 明确作品立意与最低目标 | 已完成 | 已收敛为端云语音闭环 |
| 独立项目目录整理 | 已完成 | 已从旧项目杂糅目录中拆出 `2026AIoT` |
| `.env` 与配置加载 | 已完成 | 根目录配置可稳定读取 |
| Windows 本地 wav 播放 | 已完成 | 已修复 TTS 生成 wav 的头部问题 |
| 预录 wav 录音模块 | 已完成 | 可生成标准 `16k / mono / 16bit wav` |
| `--input-text -> LLM -> TTS -> 播放` | 已完成 | 已实测通过 |
| DashScope 原生 TTS 接入 | 已完成 | 使用 `SpeechSynthesizer`，已实测通过 |
| DashScope 原生 ASR 接入 | 已完成 | 已改为 `TranslationRecognizerChat` |
| `--input-wav` 基础链路 | 已完成 | 代码链路已打通 |
| `--record-seconds` 预录音链路 | 已完成 | 已能录音并进入 ASR |
| DeepSeek 流式输出 | 已完成 | `stream_reply()` 已接入 |
| 分句流式 TTS 播报 | 已完成 | 已实测文本分句播放通过 |
| `--realtime-seconds` 实时入口 | 已完成 | 实时骨架已接入 |
| `pyaudio` 依赖安装 | 已完成 | 已装入 `AIoT` conda 环境 |
| 实时麦克风单轮识别稳定性 | 进行中 | 1 秒测试已可进入 ASR，但短语音无文本 |
| 实时模式异常清理 | 进行中 | 结束时仍有 Windows 句柄告警 |
| 持续唤醒监听设计 | 进行中 | 需补专项方案文档 |
| 板端实机联调 | 未开始 | 本地稳定后同步到板端 |
| 多音色切换 | 未开始 | 当前默认 `longwan` |
| 一键启动脚本 | 未开始 | 后续展示阶段处理 |

## 阶段进展

### 阶段 A：命令行最小闭环

状态：已完成

已完成内容：

- `--input-text`
- `--input-wav`
- `--record-seconds`
- DashScope 原生 ASR / TTS 接入
- DeepSeek 单轮问答
- Windows 播放链路修复

### 阶段 B：实时语音闭环

状态：进行中

已完成内容：

- `--realtime-seconds` 入口
- `pyaudio` 安装
- 流式 DeepSeek 输出
- 分句流式 TTS 播放

当前阻塞：

- 实时 ASR 需要继续做 3 到 5 秒真实说话验证
- Windows 退出时仍有句柄清理告警

### 阶段 C：持续唤醒监听

状态：未开始

计划内容：

- 持续监听主循环
- 唤醒触发
- 会话状态机
- 播放与录音互斥控制

## 风险记录

| 风险 | 状态 | 处理方式 |
|---|---|---|
| 实时 ASR 对短语音不稳定 | 已发现 | 用 3 到 5 秒真实语音继续验证 |
| Windows 退出时句柄清理异常 | 已发现 | 调整实时 ASR 清理逻辑 |
| TTS 流式切分可能过碎 | 已发现可能性 | 后续根据试听结果调整切句规则 |
| 板端与 Windows 声卡行为不一致 | 未验证充分 | 本地通过后尽快转板端联调 |
| 持续监听阶段音频资源冲突 | 未开始 | 第三阶段引入状态机管理 |

## 里程碑

1. 最小文本闭环跑通
2. 预录 wav 闭环跑通
3. 实时麦克风单轮闭环跑通
4. 持续唤醒监听版本跑通
5. 展示增强版本完成

## 最近一次更新

- 今日完成：
  - 将语音项目独立整理到 `2026AIoT`
  - 修复 TTS 生成 wav 头错误导致的无声问题
  - 将 ASR 改回 DashScope 原生 SDK
  - 新增预录音模块
  - 新增实时语音入口
  - 接入流式 DeepSeek 与分句流式 TTS
  - 安装 `pyaudio`
- 进行中：
  - 实时麦克风单轮闭环稳定性验证
  - 实时模式清理逻辑修复
  - 持续唤醒监听开发计划整理
- 阻塞：
  - 实时模式结束后存在 Windows 句柄告警
  - 尚未完成 3 到 5 秒真实语音连续成功验证
- 明日计划：
  - 真实跑通 `--realtime-seconds 5`
  - 修复实时模式退出告警
  - 开始持续唤醒监听状态机开发
